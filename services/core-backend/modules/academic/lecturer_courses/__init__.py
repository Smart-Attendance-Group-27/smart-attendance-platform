"""Read-only academic data (courses, timetable) for the authenticated lecturer.

Lecturers view assigned academic data; they never create or edit it here —
administrators own that (modules/academic/admin_academic, Stage 5).
"""
