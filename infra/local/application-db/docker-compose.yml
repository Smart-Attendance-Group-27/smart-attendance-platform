name: uniattend-local-application-db

services:
  application-db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${APP_DB_NAME:-uniattend}
      POSTGRES_USER: ${APP_DB_USER:-uniattend}
      POSTGRES_PASSWORD: ${APP_DB_PASSWORD:?Set APP_DB_PASSWORD in the untracked application-db .env file}
    ports:
      - "127.0.0.1:${APP_DB_PORT:-5434}:5432"
    volumes:
      - application_db_data:/var/lib/postgresql/data
      - ../../../database/smart_attendance_db_clean.sql:/docker-entrypoint-initdb.d/001_schema.sql:ro
      - ../../../database/migrations/0001_add_keycloak_user_id.sql:/docker-entrypoint-initdb.d/002_keycloak_user.sql:ro
      - ../../../database/migrations/0002_add_session_geofence_snapshot.sql:/docker-entrypoint-initdb.d/003_geofence_snapshot.sql:ro
      - ../../../database/migrations/20260814_add_qr_batch_mode.sql:/docker-entrypoint-initdb.d/004_qr_batch_mode.sql:ro
      - ../../../database/migrations/20260920_01_attendance_lifecycle_columns.sql:/docker-entrypoint-initdb.d/005_attendance_lifecycle_columns.sql:ro
      - ../../../database/migrations/20260920_02_attendance_check_in_state.sql:/docker-entrypoint-initdb.d/006_attendance_check_in_state.sql:ro
      - ../../../database/migrations/20260920_03_final_attendance_constraints.sql:/docker-entrypoint-initdb.d/007_final_attendance_constraints.sql:ro
      - ../../../database/smart_attendance_seed.sql:/docker-entrypoint-initdb.d/008_seed.sql:ro
      - ./demo_seed.sql:/docker-entrypoint-initdb.d/009_geofence_demo.sql:ro
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}",
        ]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  application_db_data:
